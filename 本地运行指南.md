# 本地运行指南：抓取脚本怎么用

> 本文所有命令均在 **Windows / Git Bash / PowerShell** 下实测过。
> 文中 `<PY>` 指代"装了 playwright 的那个 Python"，本机是：
> `C:/Users/Dong/AppData/Local/Programs/Python/Python312/python.exe`
>
> 想省事就在每个终端窗口先设一次变量：
> - cmd：`set PY=C:\Users\Dong\AppData\Local\Programs\Python\Python312\python.exe`
> - PowerShell：`$env:PY="C:\Users\Dong\AppData\Local\Programs\Python\Python312\python.exe"`
> - Git Bash：`export PY="C:/Users/Dong/AppData/Local/Programs/Python/Python312/python.exe"`
>
> 下文统一写成 `%PY%`（cmd）／`$PY`（bash），按你的终端替换即可。

---

## 0. 最快上手（三条命令）

```bash
cd /c/Users/Dong/spare-parts-monitor          # 所有命令都在仓库根目录执行

$PY -m crawler.run                            # ① 全量抓取（25 个 brand×country）
$PY api/server.py                             # ② 起服务，浏览器开 http://localhost:8000
$PY monitor.py --report                       # ③ 看这次抓得健康不健康
```

当前库内已有 2026Q3 数据：5 品牌 / 7 国家 / 5876 机型 / 48110 条价行。
所以现在直接跑 ①，绝大多数区域会**秒跳过**（断点续跑），下文会解释为什么、以及怎么强制重抓。

---

## 1. 环境准备（第一次跑必做）

### 1.1 依赖只有一个

`requirements.txt` 的内容就一行：`playwright>=1.40`。

```bash
$PY -m pip install -r requirements.txt
$PY -m playwright install chromium
```

> 验证是否已装好：
> ```bash
> $PY -c "import playwright; print(playwright.__file__)"
> $PY -c "import crawler.core; print('core OK')"     # 依赖齐全会打印 core OK
> ```

### 1.2 ⚠️ 最大的坑：别用错 Python

本机有多个 Python，**只有系统 Python 3.12 装了 playwright**：

| 解释器 | playwright | 能不能跑抓取 |
| --- | --- | --- |
| `C:/Users/Dong/AppData/Local/Programs/Python/Python312/python.exe` | ✅ 1.62 + chromium-1234 | ✅ **用这个** |
| 默认 `python`（WorkBuddy 托管 3.13.12） | ❌ 未安装 | ❌ 会退出并提示 |

用错解释器不会报奇怪的错，而是直接一行友好提示后退出：

```
ERROR: playwright 未安装。请先运行: pip install playwright && playwright install chromium
```

### 1.3 executor.py 这个外部依赖

浏览器抓取的执行器 `executor.py` 目前仍放在
`C:/Users/Dong/.workbuddy/skills/spare-parts-price/scripts/`（跟 skill 一起迭代），
由 `crawler/core.py` 注入 `sys.path`。

- **本机**：skill 在，直接能跑，不用管。
- **换机器 / 别人 clone**：会报 `ModuleNotFoundError: executor`。两个办法：
  1. 把那份 `executor.py` 拷到仓库 `vendor/` 下 —— **仓库副本优先级最高，实测立刻生效**
     （已验证：放进 `vendor/executor.py` 后 `import executor` 解析位置变为仓库内）。
     代价是会和 skill 版本漂移，建议同时把 `tools/sync_skill_deps.py` 的同步清单扩上去。
  2. 或者保留 `~/.workbuddy/skills/spare-parts-price/` 目录结构。

其余依赖（KB 配方 `references/kb/`、标定数据 `references/calibration/`、金额解析 `vendor/normalize.py`）
已经同步进仓库，**不需要 skill 也能跑**。

---

## 2. 抓哪些：品牌 × 国家清单

定义在 `crawler/run.py` 顶部的 `SCOPE`，共 **25 个区域**：

| 品牌 | 国家代码 | 需要浏览器？ |
| --- | --- | --- |
| oppo | cn, de, tr, mx, my, jp, ae | 否（纯 HTTP） |
| vivo | my, tr, ae | 是 |
| xiaomi | cn | 是 |
| apple | de, jp, ae, my | 是 |
| samsung | de, tr, my, jp, ae | de/my 走 JSON 接口；tr/jp/ae 走网页表解析 |
| google | de, jp, ae, my, tr | 是（**必须代理，国内不可达**） |

> "要不要浏览器"由 `_job_needs_browser()` 自动判定，不用你指定。
> 判定规则：读 KB 里该区域的 `query.mode` 与 `api.fetch_mode`，失败时保守当作需要浏览器。

---

## 3. 抓取命令全集

入口统一是 `python -m crawler.run`，参数只有 4 个：

```bash
$PY -m crawler.run --help
```

```
usage: run.py [-h] [--brand BRAND] [--country COUNTRY]
              [--concurrency CONCURRENCY] [--force]
```

| 参数 | 默认 | 作用 |
| --- | --- | --- |
| `--brand` | 全部 | 只抓某个品牌：`oppo` / `vivo` / `xiaomi` / `apple` / `samsung` / `google` |
| `--country` | 全部 | 只抓某个国家：`cn` `de` `tr` `mx` `my` `jp` `ae` |
| `--concurrency` | 2 | 无浏览器区域的区域级并发度，夹逼到 `[1,8]`；也可用环境变量 `RUN_ALL_CONCURRENCY` |
| `--force` | 关 | 忽略断点续跑，**本季已抓过的机型也重新取价** |

### 3.1 全量（季度更新就跑这个）

```bash
$PY -m crawler.run
```

### 3.2 单个区域（推荐：调 KB、改完自愈逻辑后验证用）

```bash
$PY -m crawler.run --brand oppo --country my
$PY -m crawler.run --brand apple --country de
$PY -m crawler.run --brand google --country de       # 需先挂代理
```

### 3.3 强制重抓（数据坏了 / 官网改版后刷新）

```bash
$PY -m crawler.run --brand oppo --country my --force
$PY -m crawler.run --force                            # 全量强制，慢，慎用
```

### 3.4 调并发

```bash
$PY -m crawler.run --concurrency 4                    # 无浏览器区域并发 4
RUN_ALL_BROWSER_CONCURRENCY=2 $PY -m crawler.run      # 浏览器区域也并发（见下方警告）
```

---

## 4. 断点续跑：为什么重跑什么都没抓到

这是最容易让人以为"脚本坏了"的行为。

**规则**：本季度已经取到价的机型，**直接从待抓列表里剔除，连请求都不发**。
本季指的是 `db.this_quarter()`，**现在是 `2026Q3`**。

实测证据（同一台机器连着跑两次 `--brand oppo --country my`）：

| 命令 | 输出 | 耗时 |
| --- | --- | --- |
| `--brand oppo --country my` | `[resume] oppo/my 本季已抓到价 298 台，不再重复取价` → `[done] oppo/my 本季新增 0 机型 / 0 条价` | **3.0s** |
| `--brand oppo --country my --force` | `[done] oppo/my 本季新增 124 机型 / 867 条价` | **40.8s** |

浏览器类品牌同理 —— apple/de 本季已抓时，仍会过一遍"自动发现"（要起 Chromium），
确认全部已抓后跳过写入：

```
  [multi-category] apple/de 品类=phone     [discover] 自动发现 31 个机型   [done] 0 机型 / 0 条价
  [multi-category] apple/de 品类=tablet    [discover] 自动发现 46 个机型   [done] 0 机型 / 0 条价
  [multi-category] apple/de 品类=watch     [discover] 自动发现 93 个机型   [done] 0 条价
  [region] ■ apple/de 耗时 64.4s
```

**幂等性已验证**：`--force` 重跑后全平台本季价行仍是 **48110**，没有重复堆积。

> 所以要抓「下个季度」的数据，等到自然跨季即可（`this_quarter()` 会自动变），或用 `--force` 覆盖当前季。

---

## 5. 代理配置

`crawler/core.py:get_proxy()` 会按以下优先级自动取，**不用改代码**：

1. 环境变量：`HTTPS_PROXY` / `HTTP_PROXY` / `ALL_PROXY`（或小写）
2. Windows 系统代理（读注册表，开了 Clash / v2rayN 的"系统代理"就自动生效）
3. 都没有 → 直连

启动日志第一行会告诉你有没有生效：`[start] 季度=2026Q3 代理=on/off ...`

```bash
# Git Bash
export HTTPS_PROXY=http://127.0.0.1:7890
# SOCKS5
export ALL_PROXY=socks5://127.0.0.1:1080
```

> Google 全域在国内不可达，**必须代理**；不挂代理时它的 5 个区域会静默 0 机型，
> 这也是当前 5 条待修问题的全部来源。

---

## 6. 结果怎么看

### 6.1 先看一眼健康度（最省事）

```bash
$PY monitor.py            # 巡检 + 把失败项补写进 maintenance_queue
$PY monitor.py --report   # 只打印摘要，不写队列
```

实跑输出（节选）：

```
=== 监控巡检 2026-09-20T15:30:36 共 25 个 brand/country ===
  [SKIP] google/de 状态=skipped 行数=0
  [OK] vivo/my 状态=success 行数=1139
  [OK] xiaomi/cn 状态=success 行数=21244
  [OK] oppo/my 状态=success 行数=867
=== 无新待修项 ===
```

> 注意 `[SKIP]` 是"本季已抓跳过"，不是失败；行数 0 是**本次运行**新增 0 条，历史数据仍在。

当前本季各区域价行数：

```
oppo    ae 2894  cn 3749  de 2410  jp 2439  mx 2910  my 2828  tr 2664
vivo    ae  766  my 1139  tr  455
xiaomi  cn 21264
samsung ae   88  de 1139  jp  215  my  333  tr  157
apple   ae  455  de  455  jp  447  my  455
google  —（5 个区域均无数据，待修队列中）
```

### 6.2 起服务看页面

```bash
$PY api/server.py                 # 默认 :8000
$PY api/server.py --port 8010     # 换端口
PORT=8010 $PY api/server.py       # 环境变量也行（命令行优先）
```

服务端自带静态托管，浏览器打开 `http://localhost:8000` 即可，不需要 npm/node/Vite。

### 6.3 直接查库

库文件 `spare_parts.db`，关键表：`models` / `parts` / `price_snapshots` / `run_logs` / `maintenance_queue`。

```bash
$PY -c "
import sqlite3
c = sqlite3.connect('spare_parts.db')
print(c.execute('SELECT COUNT(*) FROM price_snapshots WHERE quarter=?', ('2026Q3',)).fetchone()[0])
for r in c.execute('SELECT brand,country,status,rows_written,finished_at FROM run_logs ORDER BY id DESC LIMIT 5'):
    print(r)
"
```

按区域统计：

```bash
$PY -c "
import sqlite3
c = sqlite3.connect('spare_parts.db')
for bid, cc, n in c.execute('''SELECT m.brand_id, m.country_code, COUNT(DISTINCT ps.id)
      FROM price_snapshots ps JOIN parts p ON p.id=ps.part_id JOIN models m ON m.id=p.model_id
      WHERE ps.quarter=? GROUP BY 1,2 ORDER BY 1,2''', ('2026Q3',)):
    print(c.execute('SELECT name FROM brands WHERE id=?', (bid,)).fetchone()[0], cc, n)
"
```

---

## 7. 失败与重跑

```bash
$PY monitor.py                              # 1) 巡检，失败项进 maintenance_queue
$PY tools/refetch_failing.py                # 2) 预演：列出待重抓区域（不写库）
$PY tools/refetch_failing.py --apply        # 3) 真正重抓
$PY tools/refetch_failing.py --apply --vivo-only   # 只重抓 vivo（避免重复打 mi.com）
```

> 说明：`refetch_failing.py` 只修 **vivo / xiaomi** 的过期或错位价格
> （删本季旧快照后用官方接口价重插），不是通用全品牌重抓工具；
> 其他品牌请用 `$PY -m crawler.run --brand X --country Y --force`。

除了命令行，服务也能触发抓取（后台起子进程，需 token）。
注意 **参数走 URL query，不是 JSON body**（源码用 `parse_qs` 读 query）：

```bash
$PY api/server.py &

# 触发（brand / country 可省略表示全部，force=1 等价 --force）
curl -X POST "http://127.0.0.1:8000/api/crawl?brand=oppo&country=my&token=spareparts-crawl-2026"
curl -X POST "http://127.0.0.1:8000/api/crawl?force=1&token=spareparts-crawl-2026"

# 看进度（返回最近 20 行日志 tail）
curl "http://127.0.0.1:8000/api/crawl/status?token=spareparts-crawl-2026"
```

实跑校验：非法品牌会被挡下并返回 400
`{"error": "未知品牌：nope（可用值见 /api/brands 的 name 字段）"}`，
`/api/crawl/status` 正常返回 200 —— 说明通道与鉴权都通。

> 默认 token 是占位串 `spareparts-crawl-2026`；**公网部署务必用环境变量覆盖**：
> `CRAWL_TOKEN=<随机强串> $PY api/server.py`

官网选择器改版导致抽不到价时，重跑标定脚本（全部已改为相对路径）：

```bash
cd references/calibration
$PY google_calibrate.py --regions de jp     # 重写 google_locators.json 并回写 KB
$PY oppo_tr_calibrate.py --model "OPPO Ace2"
$PY vivo_calibrate.py                       # 截图落 output/evidence/
```

```bash
$PY tools/sync_skill_deps.py --check
```

---

## 8. 季度更新 / 定时运行

```bash
$PY run_quarterly.py                        # 全量抓取 → 算异动 → 推送
$PY push.py --quarter 2026Q3 --threshold 0.15   # 只算异动/推送，不抓
```

推送受 `config.push.json` 控制（模板见 `config.push.json.example`），
`enabled=false` 时只本地预览不发送：

```json
{ "enabled": false, "alert_threshold": 0.15,
  "wechat": {"webhook_url": ""}, "email": {"smtp_host": "", "to": []} }
```

Windows 计划任务（每季度一次）：

```bat
schtasks /create /tn "SparePartsQuarterly" /tr "<仓库目录>\run_quarterly.bat" /sc MONTHLY /mo 3 /d 1 /st 03:00
```

`refresh_crawl.bat` 是一键「杀旧服务 → 重抓 → 重启服务」，根目录由 `%~dp0` 推导，
解释器默认用 PATH 里的 `python`，可用 `set PY_EXE=<你的 python.exe>` 覆盖。

---

## 9. 时间预算 & 并发建议

| 场景 | 实测耗时 |
| --- | --- |
| oppo/my 断点跳过 | 3.0s |
| oppo/my `--force`（124 机型 / 867 条价） | 40.8s |
| apple/de 三品类"仅发现"（31+46+93 机型） | 64.4s |
| vivo/my 完整抓（历史记录） | ~306s |

并发模型写在 `run_all()` 的 docstring 里，别乱调：

- **无浏览器池**（oppo 7 区 + samsung de/my）：默认并发 **2**，`--concurrency` 或 `RUN_ALL_CONCURRENCY`。
  实测 OPPO CDN 在并发 8 附近就饱和了（c=8→4.45×、c=12→4.06×），夹逼上限 8。
- **浏览器池**（vivo / xiaomi / apple / samsung tr,jp,ae / google）：**默认串行**。
  理由不是保守，是踩过坑：并发时 vivo/tr 报 `Page.goto: Timeout 25000ms` → **自动发现 0 机型**，
  这种失败不报错、只静默丢数据，比慢危险得多。
  要放开具设 `RUN_ALL_BROWSER_CONCURRENCY`（上限 4），此时每个浏览器区域会各起独立 Chromium 避免连坐。

---

## 10. 排障速查

| 症状 | 原因 | 怎么办 |
| --- | --- | --- |
| `ERROR: playwright 未安装...` | 用了没装 playwright 的解释器 | 换 `$PY`（系统 Python 3.12）；或 `$PY -m pip install playwright && $PY -m playwright install chromium` |
| `ModuleNotFoundError: executor` | 机器上没有 skill 目录 | 把 `executor.py` 拷到 `vendor/`（见 1.3） |
| 跑完 `[done] ... 0 机型 / 0 条价` | 本季断点续跑跳过，**正常** | 想重抓加 `--force` |
| 某区域一直 0 机型、也不报错 | 浏览器并发顶超时 / 被墙 | 保持默认串行；挂代理后重跑该区域 |
| `sqlite3.OperationalError: no such column: price_rows` | 表名不对 | 真实表名是 `price_snapshots`、`parts`、`models`，见第 6 节 |
| Google 5 区全部失败 | 国内不可达 | 挂代理或跳过：`$PY -m crawler.run --brand oppo` |
| 想恢复旧数据 | — | `spare_parts.db.bak_*` 保留最新 3 个回滚点 |

---

## 附：目录职责速查

```
crawler/run.py            抓取总入口（SCOPE、并发池、断点续跑都在这）
crawler/core.py           代理感知的浏览器启动 + 路径注入
crawler/samsung_api.py    三星 de/my JSON 接口、tr/jp/ae 表解析
references/kb/*.json      每个品牌×国家的抓取配方（这是调站点的第一现场）
references/calibration/   标定脚本与样本数据（已全部相对路径）
vendor/normalize.py       金额解析唯一实现，勿另写
api/server.py             后端接口 + 静态托管前端
db.py                     建表 / upsert / 汇率 / 运行日志
monitor.py                巡检与异常检测
tools/                    排障、重抓、核查工具
```
